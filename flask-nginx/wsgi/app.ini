[uwsgi]
chdir = /packages/
base =/packages/
module = example.bootstrap
callable = application

master = true 

uid = www
gid = www

socket = /tmp/app.sock
chown-socket = www:www
chmod-socket = 660

processes = 2
threads = 4

logger = file:/tmp/uwsgi.log
